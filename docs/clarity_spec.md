# CLARITY — Техническая спецификация

## 🎯 Концепция проекта

Система управления личными финансами с AI-помощником, которая не просто считает расходы, а **учит финансовой грамотности** через:
- Автоматическую категоризацию транзакций (ML)
- Оценку финансового здоровья по шкале 0-100
- **AI-чат консультант** с полным контекстом финансов пользователя
- Персонализированные прогнозы и рекомендации

---

## 🏆 КИЛЛЕР-ФИЧИ

### 1. Financial Health Score (0-100)
Комплексная оценка финансового состояния с 4 компонентами:

**Компоненты (взвешенная сумма):**
- **Savings Rate (30%)** — процент накоплений от дохода
- **Emergency Fund (25%)** — размер финансовой подушки в месяцах
- **Spending Stability (25%)** — стабильность расходов (низкая вариация = хорошо)
- **Essential Ratio (20%)** — баланс обязательных/необязательных трат (норма 50-60%)

**Оценка (Grade):**
- A: 90-100 (Отлично)
- B: 75-89 (Хорошо)
- C: 60-74 (Удовлетворительно)
- D: 40-59 (Плохо)
- F: 0-39 (Критично)

**UI:** Большая анимированная цифра на Dashboard с детализацией компонентов.

---

### 2. AI Financial Assistant (Чат-помощник) 🤖

**Концепция:**
AI-помощник с полным контекстом финансов пользователя, который отвечает на вопросы и дает советы на основе реальных данных.

**Что знает AI:**
- Полная история транзакций пользователя
- Текущий баланс и финансовая подушка
- Структура доходов/расходов по категориям
- Health Score и его компоненты
- Тренды за последние месяцы

**Примеры вопросов:**

**Ситуационные прогнозы:**
- "Меня уволили с работы. На сколько хватит моих накоплений?"
- "Хочу накопить 100000₽ за 3 месяца. Реально ли это?"
- "Планирую отпуск на 50000₽ через 2 месяца. Смогу ли я накопить?"
- "Мне предложили работу с зарплатой 150000₽. Как изменится моя ситуация?"

**Анализ:**
- "На что я трачу больше всего денег?"
- "Почему мой Health Score упал до 65?"
- "Где я могу сэкономить?"
- "Сколько у меня финансовая подушка в месяцах?"

**Советы:**
- "Как мне улучшить финансовое здоровье?"
- "Посоветуй, как оптимизировать мои расходы"
- "Что делать, если я потеряю работу?"

**Как работает:**

1. **Frontend:** Пользователь пишет в чат
2. **Backend:** Получает сообщение + собирает контекст:
   - Последние 100 транзакций
   - Health Score
   - Аналитика по категориям
   - Баланс и финансовая подушка
3. **Backend → ML Service:** Отправляет POST /chat с промптом:
   ```
   System: Ты финансовый консультант. У тебя есть данные пользователя...
   
   User Context:
   - Баланс: 45000₽
   - Финансовая подушка: 1.2 месяца (рекомендуем 3-6)
   - Health Score: 68/100 (Grade: C)
   - Средний доход: 120000₽/мес
   - Средние расходы: 85000₽/мес
   - Топ категории: Продукты (25000₽), Жилье (30000₽), Рестораны (12000₽)
   - Тренд: расходы растут на 5%/мес
   
   User Question: "Меня уволили. На сколько хватит денег?"
   
   Ответь конкретно, с цифрами и советами.
   ```
4. **ML Service (LLM):** Генерирует ответ через свою модель
5. **ML Service → Backend → Frontend:** Возвращает ответ в чат

**Технически:**
- Своя LLM модель (обучена на предоставленном датасете)
- LLM развернута как часть ML сервиса (Python)
- Контекст формируется на Backend (SQL агрегации)
- Backend → ML Service → LLM → ответ
- История чата хранится в БД (для контекста разговора)
- Потоковый ответ (streaming) опционально

---

## 📊 ОСНОВНОЙ ФУНКЦИОНАЛ

### 1. Авторизация
- Регистрация (email + password)
- Вход (JWT токен)
- Все данные привязаны к user_id

### 2. Транзакции (CRUD)
- Добавить транзакцию → ML автоматически определяет категорию
- Список транзакций (фильтры: месяц, категория, тип)
- Редактировать категорию вручную
- Удалить транзакцию
- Пометка "обязательный платеж"

### 3. ML Категоризация
**Вход:** описание транзакции + сумма
**Выход:** категория + confidence

Категории:
```
Продукты, Рестораны, Транспорт, Жилье, Здоровье, 
Развлечения, Одежда, Образование, Связь, Спорт, 
Путешествия, Подарки, Другое
```

### 4. Аналитика

**Summary (сводка за месяц):**
- Общий доход / расход / баланс
- Savings rate
- Расходы по категориям
- Обязательные vs необязательные расходы

**Trends (тренды по месяцам):**
- Доходы/расходы/баланс за N месяцев
- Визуализация: line chart, bar chart

### 5. Прогнозирование (ML)
**Вход:** история транзакций
**Выход:** прогноз на следующий месяц
- Ожидаемый доход
- Ожидаемые расходы
- Ожидаемые накопления
- Размер финансовой подушки

**Методы:** LSTM / Prophet / простая скользящая средняя

### 6. Рекомендации (ML)
**Вход:** структура расходов
**Выход:** персонализированные советы
- Приоритет (high/medium/low)
- Категория
- Текст рекомендации
- Потенциальная экономия

**Логика:**
- Сравнение с нормами (Рестораны <10% дохода)
- Выявление аномалий (трата в 3x больше средней)
- Проверка финансовой подушки (<3 месяцев = предупреждение)

---

## 🗄️ База данных (PostgreSQL)

### Таблица: users
```sql
id (serial, PK)
email (varchar, unique)
password_hash (varchar)
created_at (timestamp)
```

### Таблица: transactions
```sql
id (serial, PK)
user_id (integer, FK → users.id)
amount (decimal)
description (varchar)
category (varchar)
date (timestamp)
type (varchar) -- 'income' / 'expense'
is_essential (boolean)
created_at (timestamp)

INDEX: user_id, date, category
```

### Таблица: chat_messages
```sql
id (serial, PK)
user_id (integer, FK → users.id)
role (varchar) -- 'user' / 'assistant'
content (text)
created_at (timestamp)

INDEX: user_id, created_at
```

---

## 🔌 API Endpoints

### Авторизация
- `POST /api/register` — регистрация
- `POST /api/login` — вход (возвращает JWT)

### Транзакции (требуют JWT)
- `POST /api/transactions` — создать
- `GET /api/transactions` — список (query: limit, offset, month)
- `PATCH /api/transactions/:id` — изменить категорию
- `DELETE /api/transactions/:id` — удалить

### Аналитика
- `GET /api/analytics/summary?month=2024-12`
- `GET /api/analytics/trends?months=6`

### Финансовое здоровье
- `GET /api/health-score?month=2024-12`

### Прогноз и рекомендации
- `POST /api/predict` — прогноз (body: months_ahead)
- `POST /api/recommend` — рекомендации

### AI Чат
- `POST /api/chat` — отправить сообщение
  - Body: `{"message": "Меня уволили. На сколько хватит денег?"}`
  - Backend собирает контекст → LLM → ответ
  - Response: `{"reply": "...", "context_used": {...}}`
- `GET /api/chat/history` — история чата

---

## 🤖 ML Service (Python)

### Endpoints
- `POST /categorize` — категоризация транзакции
- `POST /predict` — прогноз на месяц
- `POST /recommend` — рекомендации
- `POST /chat` — AI ассистент (LLM)

### Модели
- **Категоризация:** TF-IDF + Logistic Regression / DistilBERT
- **Прогноз:** LSTM / Prophet / Moving Average
- **Рекомендации:** Rule-based система с нормами
- **AI Чат:** Fine-tuned LLM на датасете (GPT-2 / LLaMA / Mistral)

---

## 🎨 Frontend (React)

### Страницы
1. **Login / Register** — авторизация
2. **Dashboard** — главная
   - Health Score карточка (большая анимация)
   - 4 KPI карточки (баланс, доходы, расходы, накопления)
   - Графики: тренды, категории
   - Последние 5 транзакций
3. **Транзакции** — таблица с CRUD
4. **Аналитика** — графики, прогноз
5. **Рекомендации** — карточки с советами
6. **AI Чат** — чат-интерфейс с помощником 🤖

### Библиотеки
- shadcn/ui (UI компоненты)
- recharts (графики)
- axios (HTTP клиент с JWT)
- framer-motion (анимации)
- lucide-react (иконки)

---

## ⚙️ Технический стек

**Backend:** Go 1.25.3, Gin, GORM, PostgreSQL, JWT, yagalog
**ML Service:** Python, FastAPI, scikit-learn / PyTorch, transformers (HuggingFace)
**LLM:** Fine-tuned модель на предоставленном датасете (GPT-2 / LLaMA / Mistral)
**Frontend:** React, TypeScript, Vite, Tailwind CSS

---

## 🏗️ Архитектура системы

```
┌──────────────┐
│   Frontend   │
│   (React)    │
└──────┬───────┘
       │ HTTP + JWT
       ↓
┌──────────────┐      ┌──────────────────┐
│   Backend    │ ───→ │   PostgreSQL     │
│     (Go)     │      └──────────────────┘
└──────┬───────┘
       │ HTTP
       ↓
┌──────────────────────────────┐
│      ML Service (Python)     │
│                              │
│  ┌────────────────────────┐ │
│  │ Категоризация (BERT)   │ │
│  ├────────────────────────┤ │
│  │ Прогноз (LSTM)         │ │
│  ├────────────────────────┤ │
│  │ Рекомендации (Rules)   │ │
│  ├────────────────────────┤ │
│  │ LLM Chat (Fine-tuned)  │ │
│  └────────────────────────┘ │
└──────────────────────────────┘
```

**Поток данных для AI чата:**
1. User → Frontend (чат)
2. Frontend → Backend POST /api/chat
3. Backend собирает контекст (SQL запросы)
4. Backend → ML Service POST /chat (контекст + сообщение)
5. ML Service → LLM генерация ответа
6. ML Service → Backend → Frontend ответ
7. Backend сохраняет историю в БД

---

## 📋 Задачи для команды

### Backend (16 часов)
1. **Auth (3ч):** JWT, регистрация, вход, middleware
2. **CRUD транзакций (3ч):** 4 эндпоинта + интеграция с ML
3. **Аналитика (3ч):** summary, trends (SQL агрегации)
4. **Health Score (3ч):** расчет 4 компонентов + API
5. **AI Чат (4ч):**
   - Эндпоинт `/api/chat`
   - Сбор контекста (SQL запросы)
   - Интеграция с LLM API
   - Сохранение истории чата

### ML (16 часов)
1. **Категоризация (5ч):** обучить модель на датасете транзакций
2. **Прогноз (3ч):** LSTM / простая модель
3. **Рекомендации (2ч):** rule-based логика
4. **AI Чат LLM (6ч):**
   - Подготовка датасета для fine-tuning
   - Fine-tune модель (GPT-2 / LLaMA / Mistral) на диалогах
   - Эндпоинт `/chat` в FastAPI
   - Загрузка модели при старте сервиса
   - Оптимизация inference

**Датасет LLM:**
- Предоставлен организаторами
- Содержит примеры финансовых консультаций
- Формат: вопрос-ответ с контекстом

**Требования к LLM:**
- Быстрый inference (<2 сек на ответ)
- Размер модели: оптимально 1-7B параметров (для CPU/GPU inference)
- Формат ответа: конкретный, с цифрами, без "воды"
- Модель должна работать с русским языком

### Frontend (8 часов)
1. **Auth (1ч):** Login/Register формы, JWT хранение
2. **Health Score Card (1.5ч):** компонент с анимацией
3. **Dashboard (2ч):** KPI карточки + графики
4. **Транзакции (1.5ч):** таблица + CRUD формы
5. **AI Чат (2ч):** чат-интерфейс, обработка ответов

**Итого: 40 часов**
- Backend: 16ч
- ML: 16ч (включая LLM)
- Frontend: 8ч

---

## 🎯 Минимальный MVP

**Обязательно (если не успеваем все):**
1. Авторизация
2. CRUD транзакций + ML категоризация
3. Dashboard с Health Score
4. **AI Чат** (даже если модель отвечает не идеально)
5. Базовая аналитика

**Можно упростить:**
- Прогноз → простое среднее (не ML)
- Рекомендации → 2-3 rule-based
- LLM → использовать базовую модель без fine-tuning (если не успели обучить)
- Чат → без истории (stateless, каждый запрос независимый)

**План B для LLM (если не успели fine-tune):**
- Использовать готовую multilingual модель (mGPT, ruGPT)
- Хорошо составленный промпт с контекстом
- Все равно будет работать, просто менее точно

---

## 🏆 Почему это победит

**Уникальные фичи:**
1. **Health Score** — никто не делает комплексную оценку 0-100
2. **AI Чат** — ситуационные прогнозы ("уволили", "как накопить")
3. **Образовательный эффект** — учит, а не просто считает

**На презентации:**
1. Показываем Dashboard с Health Score 73/100 (анимация)
2. Открываем чат, пишем: *"Меня уволили. На сколько хватит денег?"*
3. AI отвечает с конкретными цифрами: *"При расходах 85000₽/мес ваших 45000₽ хватит на 16 дней. Рекомендую..."*
4. Жюри: 🤯

**Покрывает требования из PDF:**
- ✅ "Образовательный эффект"
- ✅ "Повышение финансовой грамотности"
- ✅ "Креативный подход"
- ✅ "Персонализированные рекомендации"

---

## 💡 Технические детали AI Чата

### Промпт для LLM (пример)

```
System: Ты финансовый консультант Clarity. Отвечай конкретно, с цифрами.

User Financial Context:
- Текущий баланс: {balance}₽
- Финансовая подушка: {cushion_months} месяца (норма: 3-6)
- Health Score: {score}/100 (Grade: {grade})
- Средний доход в месяц: {avg_income}₽
- Средние расходы в месяц: {avg_expense}₽
- Обязательные расходы: {essential_expense}₽/мес
- Топ-3 категории расходов:
  1. {category1}: {amount1}₽
  2. {category2}: {amount2}₽
  3. {category3}: {amount3}₽
- Тренд расходов: {trend_direction} на {trend_percent}%

История последних транзакций (показываются паттерны):
{last_transactions_summary}

User Question: {user_message}

Ответь максимально конкретно и практично. Если возможно, дай цифры и план действий.
```

### Формирование контекста (Backend)

**Функция:** `BuildChatContext(userID int) string`

1. Получить баланс: `SUM(CASE WHEN type='income' THEN amount ELSE -amount END)`
2. Рассчитать Health Score
3. Получить средние доходы/расходы за 3 месяца
4. Получить топ-3 категории: `GROUP BY category ORDER BY SUM(amount) DESC LIMIT 3`
5. Посчитать финансовую подушку: `баланс / средние_расходы_в_месяц`
6. Определить тренд: сравнить текущий месяц с предыдущим
7. Сформировать строку для промпта

### Интеграция с LLM

**Backend → ML Service:**
```go
type ChatRequest struct {
    Context string `json:"context"`
    Message string `json:"message"`
    History []ChatMessage `json:"history"`
}

// Отправка запроса в ML сервис
resp, err := http.Post("http://ml-service:5000/chat", 
    "application/json",
    bytes.NewBuffer(jsonData))
```

**ML Service эндпоинт `/chat`:**
```python
@app.post("/chat")
def chat(request: ChatRequest):
    # Формируем промпт с контекстом
    prompt = f"{request.context}\n\nUser: {request.message}\nAssistant:"
    
    # Генерация ответа через свою LLM
    response = model.generate(prompt, max_length=512)
    
    return {"reply": response}
```

**Модель:**
- Базовая модель: GPT-2 / LLaMA-7B / Mistral-7B
- Fine-tuning на датасете финансовых консультаций
- Inference на CPU/GPU в зависимости от ресурсов

### Хранение истории

**Зачем:** чтобы AI помнил контекст разговора

**Таблица chat_messages:**
- user_id, role, content, created_at

**При отправке:**
1. Сохранить сообщение пользователя
2. Получить последние 10 сообщений для контекста
3. Отправить в LLM
4. Сохранить ответ AI

---

## 🚀 Дополнительные фишки (если успеем)

**Быстрые команды в чате:**
- "/balance" → показать баланс
- "/summary" → сводка за месяц
- "/health" → Health Score

**Улучшения LLM:**
- Streaming ответов (постепенное появление текста)
- Поддержка контекста из прошлых сообщений
- Предложения вопросов (quick replies)

---

## 📞 Координация команды

**Backend ждет от ML:**
- ML сервис на localhost:5000
- 4 эндпоинта работают (categorize, predict, recommend, chat)
- LLM модель загружена и готова к inference
- Время ответа LLM <3 секунд

**Frontend ждет от Backend:**
- Backend на localhost:8080
- JWT токен в Authorization header
- Все эндпоинты возвращают JSON
- Чат отвечает быстро (пользователь не ждет >5 сек)

**ML команда:**
- Датасет LLM получен от организаторов
- Модель обучена/fine-tuned до начала интеграции
- Эндпоинт /chat протестирован и работает стабильно

**Все ждут:**
- Четкое соблюдение API контрактов
- Быстрая коммуникация в чате
- Деплой на сервер к концу 36 часа